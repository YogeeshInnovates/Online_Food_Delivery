<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
</head>
<style>
.profile-header {
   
background-color: #37718e;

    color: #fff;
    padding: 20px 0;
    width: 100%;
}

.profile-header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

.profile-details {
    flex: 1;
}

.edit-profile {
    flex-shrink: 0;
}

.edit-button {
    padding: 10px 20px;
    background-color: #fff;
    color: #37718e;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}
.edit-button:hover{
    background-color: pink;;
    transition: background-color 0.3s ease;
    box-shadow: 2px 4px 5px black;
}
.profile-details h2{
    font-size: 50px;
}
.content-page {
    border:  solid #37718e;
    border-width: 1px 10px 1px 10px; /* top, right, bottom, left */
    border-radius: 10px;
    margin: 0px auto;
    max-width:100%;
    padding: 20px;
    position:relative;
  top:80px;
  height:100%;

}
.content_header
{
    display:flex;
    justify-content:center;
    align-items: center;
    width:100%;
    height:90px;
    color:rgb(36, 201, 118); 
}
.content_header h1{
    font-size: 40px;
}
.buttonpress {
    height: 500px;
    width: 400px;
    background-color: aliceblue;
    box-shadow: 2px 2px 5px black;
    position: absolute;
    border-radius: 10px;
  top:150px;
    display: none;
    align-items: center; /* Center items vertically */
    justify-content: center; /* Center items horizontally */
    left: 60%;
    right:5px;
    position: fixed;
    top: 90px;
    right: -100%;
    transition: right 0.5s ease-in-out; 
}

.symbol {
    position: absolute;
    top: 10px;
    right: 10px;
}

.symbol i {
    color: red; /* Adjust the color as needed */
    font-size: 24px; /* Adjust the font size as needed */
    cursor: pointer;
}
.show {
    right: 0;
}

.buttonpress form {
    padding: 20px;
    margin: 5px;
    width: 500px;
    text-align: center;
}

.buttonpress input[type="text"],
.buttonpress input[type="mail"],
.buttonpress input[type="phonenumber"],
.buttonpress input[type="submit"] {
    width: calc(100% - 40px); /* Adjust input width */
    padding: 10px;
    margin: 25px;
    display: inline-block; /* Display inputs horizontally */
}

.buttonpress button[type="submit"] {
    width: calc(100% - 40px); /* Adjust button width */
    padding: 10px;
    margin: 5px;
    background-color: #37718e;
    color: white;
    border: none;
    cursor: pointer;
}
.list{
    height:100%;
    padding:10px;

}
.contentlist{
    border:2px solid rgb(243, 241, 241);
    height:auto;
    border-radius: 10px;
    margin-top:12px;
    box-shadow: 0 0 25px#74d162;;
    padding:10px;
    padding-left:90px;
    
}
@media only screen and (max-width: 576px) {
  .profile-header {
    padding: 10px 0; /* Decrease padding for profile header */
    width:100%;
  }

  .profile-details h2 {
    font-size: 30px; /* Decrease font size for profile name */
  }

  .profile-details h4 {
    font-size: 18px; /* Decrease font size for profile details */
  }

  .content-page {
    border-width: 5px; /* Decrease border width for content page */
    padding: 10px; /* Decrease padding for content page */
 
  }

  .content_header h1 {
    font-size: 30px; /* Decrease font size for content header */
  }

  .buttonpress {
    width: 90%; /* Adjust width for button press */
    max-width: 400px; /* Set maximum width for button press */
    margin: 0 auto; /* Center button press horizontally */
    top: 120px; /* Adjust top position */
    right: 5%; /* Adjust right position */
    left: 5%; /* Adjust left position */
  }

  .buttonpress form {
    width: 90%; /* Adjust width for form */
    max-width: 400px; /* Set maximum width for form */
    margin: 0 auto; /* Center form horizontally */
  }

  .buttonpress input[type="text"],
  .buttonpress input[type="email"],
  .buttonpress input[type="tel"] {
    width: calc(100% - 20px); /* Adjust input width */
  }
}

</style>
<body>
    <%- include('partials/profilenav.ejs') %>

    <div class="profile-header">
        <div class="profile-header-content">
            <div class="profile-details">
                <% if (signupinfo.length > 0) { %>
                    <% signupinfo.forEach(user => { %>
                        <h2><strong> <%= user.name %></h2></strong>
                        <h4><strong></strong> <%= user.mail %></h4></strong>
                        <h4><strong> <%= user.phone %></h4></strong>
                    <% }); %>
                <% } else { %>
                    <p>No user details found</p>
                <% } %>
            </div>
            <div class="edit-profile">
                <button class="edit-button" id="edit">Edit Profile</button>
               
            </div>
        </div>
    </div>
    
    <div class="content-page">
    <div class="content_header">
        <h1>Your order with TasteToDoor listed here</h1>

    </div>

    
    </div>
    <div class="buttonpress" id="buttonPress">
        <div class="symbol"><i class="fas fa-times-circle" id="remove"></i></div>
        <form action="/profile_update" method="POST" id="profileForm">
            <center><h1>Edit Profile</h1></center>
           
            <input type="text" name="username" placeholder="Username" required>
            <input type="mail" name="mail" placeholder="mail" required>
            <input type="phonenumber" name="phonenumber" placeholder="phonenumber" required>
            <button type="submit" id="submitButton">Update</button>
        </form>
    </div>
    <script>
   
  const edit=document.getElementById("edit");
    const removeButton = document.getElementById('remove');
    const buttonPress = document.getElementById('buttonPress');

    removeButton.addEventListener('click', function() {
        buttonPress.style.display = 'none';
    });
    edit.addEventListener('click', function() {
        buttonPress.style.display = 'flex';
    });
    </script>


</body>    
